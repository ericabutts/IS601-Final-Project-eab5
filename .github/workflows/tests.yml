name: CI Tests

on:
  push:
    branches:
      - module8
  pull_request:
    branches:
      - module8

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Checkout
      - uses: actions/checkout@v3

      # Set up Python
      - uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      # Install dependencies
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install playwright
          playwright install

      # Run unit & integration tests
      - run: pytest -v --cov=app tests/

      # Serve frontend and run Playwright E2E tests
      - run: |
          # Start frontend in background
          python -m http.server 5500 &
          FRONTEND_PID=$!

          # Wait until frontend is reachable
          echo "Waiting for frontend at http://localhost:5500 ..."
          for i in {1..15}; do
            if curl -s http://localhost:5500 > /dev/null; then
              echo "Frontend is up!"
              break
            fi
            sleep 1
          done

          # Run Playwright tests
          pytest -v tests/test_e2e_playwright.py

          # Kill frontend server after tests
          kill $FRONTEND_PID
